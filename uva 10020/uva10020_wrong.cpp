#include<iostream>
#include<vector>
#include<algorithm>
#include<cstdio>

using namespace std;

int main(){
    int l,r;

    while(l--){ // 錯誤：l 還沒有讀入，就拿來 while
        segment[0, M]{ // 錯誤：C++ 沒有這種寫法，而且 segment、M 都沒宣告
            if(i=1) // 錯誤：i 沒宣告，而且 = 是指定，不是比較
                cout<<
        }
    }

    return 0;
}
